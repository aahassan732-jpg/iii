import { useState } from "react";
import { Activity, Bell, CircleUserRound, Clock3, Crown, Loader2, Plus, ShieldCheck, Sparkles, UsersRound } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";

export default function Dashboard() { const { isAuthenticated, loading } = useAuth(); const [username, setUsername] = useState(""); const monitors = trpc.monitor.list.useQuery(undefined, { enabled: isAuthenticated }); const subscription = trpc.subscription.me.useQuery(undefined, { enabled: isAuthenticated }); const add = trpc.monitor.add.useMutation({ onSuccess: () => { setUsername(""); monitors.refetch(); } });
  if (loading) return <div className="py-24 text-center"><Loader2 className="mx-auto animate-spin text-[#8f6ee8]" /></div>;
  if (!isAuthenticated) return <div className="mx-auto max-w-2xl px-5 py-24 text-center"><div className="mx-auto grid h-16 w-16 place-items-center rounded-3xl bg-[#f0eaff] text-[#8f6ee8]"><CircleUserRound /></div><h1 className="mt-6 text-4xl font-black">لوحتك في انتظارك</h1><p className="mt-4 leading-8 text-[#81768c]">سجّل الدخول لإضافة حسابات للمراقبة واستقبال تنبيهات التغييرات.</p><Button onClick={() => startLogin()} className="mt-7 rounded-xl bg-[#8f6ee8] px-7">تسجيل الدخول</Button></div>;
  return <div className="mx-auto max-w-7xl px-5 py-10 lg:px-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><div className="text-sm font-bold text-[#8f6ee8]">مساحة المتابعة</div><h1 className="mt-2 text-4xl font-black">لوحتك، بكل وضوح</h1><p className="mt-2 text-[#81768c]">تابع الحسابات التي تهمك من مكان واحد.</p></div><Link href="/pricing"><Button variant="outline" className="rounded-xl border-[#e6def5]"><Crown size={16} className="ml-2 text-[#d49127]" /> ترقية الباقة</Button></Link></div><div className="mt-8 grid gap-4 md:grid-cols-3"><div className="rounded-3xl bg-[#8f6ee8] p-6 text-white shadow-xl shadow-[#8f6ee82c]"><div className="flex items-center justify-between"><span className="text-sm text-white/75">الباقة الحالية</span><Sparkles size={18} /></div><div className="mt-4 text-3xl font-black">{subscription.data?.plan ?? "FREE"}</div><div className="mt-2 text-sm text-white/75">3 تحليلات مجانية يوميًا</div></div><div className="rounded-3xl border border-[#eee6f7] bg-white p-6"><div className="flex items-center justify-between text-[#81768c]"><span>الحسابات المراقبة</span><UsersRound size={18} className="text-[#8f6ee8]" /></div><div className="mt-4 text-3xl font-black">{monitors.data?.length ?? 0}<span className="text-base font-semibold text-[#968ba0]"> / {subscription.data?.plan === "BUSINESS" ? 100 : subscription.data?.plan === "PRO" ? 20 : 1}</span></div></div><div className="rounded-3xl border border-[#eee6f7] bg-white p-6"><div className="flex items-center justify-between text-[#81768c]"><span>حالة الخدمة</span><Activity size={18} className="text-[#61b691]" /></div><div className="mt-4 flex items-center gap-2 text-2xl font-black"><span className="h-3 w-3 rounded-full bg-[#61b691]" /> جاهزة</div></div></div><div className="mt-8 grid gap-6 lg:grid-cols-[1.15fr_.85fr]"><section className="rounded-[2rem] border border-[#eee6f7] bg-white p-7 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="text-xl font-black">الحسابات المراقبة</h2><p className="mt-1 text-sm text-[#8c8198]">يعمل الفحص في الخلفية وفق تردد الباقة.</p></div><Clock3 className="text-[#8f6ee8]" /></div><div className="mt-6 flex gap-2"><Input value={username} onChange={e => setUsername(e.target.value)} placeholder="@username" className="h-12 rounded-xl bg-[#fbf9ff]" /><Button disabled={add.isPending || !username.trim()} onClick={() => add.mutate({ username: username.replace(/^@+/, ""), frequencyMinutes: subscription.data?.plan === "PRO" || subscription.data?.plan === "BUSINESS" ? 60 : 1440 })} className="h-12 rounded-xl bg-[#8f6ee8] px-5"><Plus size={17} className="ml-1" /> إضافة</Button></div><div className="mt-5 space-y-3">{(monitors.data ?? []).map(item => <div key={item.monitor.id} className="flex items-center justify-between rounded-2xl border border-[#f0e9f7] p-4"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-[#f2ecff] font-black text-[#8f6ee8]">@</div><div><div className="font-black">@{item.profile?.username}</div><div className="text-xs text-[#8e8298]">الفحص كل {item.monitor.frequencyMinutes >= 1440 ? `${Math.round(item.monitor.frequencyMinutes / 1440)} يوم` : `${item.monitor.frequencyMinutes} دقيقة`}</div></div></div><span className="rounded-full bg-[#e5f8ef] px-3 py-1 text-xs font-bold text-[#388d6d]">نشط</span></div>)}{!monitors.data?.length && <div className="rounded-2xl bg-[#fbf9ff] p-6 text-center text-sm text-[#81768c]">أضف أول حساب لتبدأ مراقبة التغييرات.</div>}</div></section><aside className="rounded-[2rem] bg-gradient-to-br from-[#fff0f7] to-[#f0eaff] p-7"><div className="flex items-center gap-2 font-black text-[#765dc2]"><Bell size={19} /> مركز التنبيهات</div><div className="mt-8 grid place-items-center rounded-2xl border border-white/70 bg-white/50 p-8 text-center"><ShieldCheck className="text-[#8f6ee8]" size={30} /><p className="mt-3 font-bold">تنبيهاتك ستظهر هنا</p><p className="mt-2 text-sm leading-7 text-[#81768c]">عند اكتشاف تغيير في نبذة أو أرقام حساب مراقب، ستصلك بطاقة واضحة داخل المنصة.</p></div></aside></div></div>;
}
