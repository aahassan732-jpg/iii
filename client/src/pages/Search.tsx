import { FormEvent, useState } from "react";
import { ArrowLeft, Loader2, Search as SearchIcon, ShieldCheck } from "lucide-react";
import { useLocation } from "wouter";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export default function Search() { const [, setLocation] = useLocation(); const [username, setUsername] = useState(""); function submit(e: FormEvent) { e.preventDefault(); const clean = username.replace(/^@+/, "").trim(); if (clean) setLocation(`/profile/${clean}`); } return <div className="mx-auto max-w-5xl px-5 py-16 lg:px-8"><div className="mx-auto max-w-3xl text-center"><div className="mx-auto mb-5 grid h-14 w-14 place-items-center rounded-2xl bg-[#f0eaff] text-[#8f6ee8]"><SearchIcon /></div><h1 className="text-4xl font-black">حلّل أي حساب عام</h1><p className="mt-4 text-lg leading-8 text-[#81768c]">أدخل اسم المستخدم كما هو في Instagram. نعرض فقط البيانات العامة التي يستطيع المصدر توفيرها.</p><form onSubmit={submit} className="mt-9 flex flex-col gap-3 rounded-2xl border border-[#e8def7] bg-white p-2 shadow-xl shadow-[#c1a8e91a] sm:flex-row"><Input autoFocus value={username} onChange={e => setUsername(e.target.value)} placeholder="@username" className="h-14 border-0 bg-[#fbf9ff] text-lg shadow-none sm:flex-1" /><Button type="submit" className="h-14 rounded-xl bg-[#8f6ee8] px-8 text-base font-bold">بدء التحليل <ArrowLeft className="mr-2" size={18} /></Button></form><div className="mt-6 flex flex-wrap justify-center gap-5 text-sm text-[#8b8095]"><span className="inline-flex items-center gap-2"><ShieldCheck size={16} className="text-[#64b694]" /> لا كلمات مرور</span><span>لا نتجاوز الحسابات الخاصة</span><span>النتيجة مؤرخة ومصدرها واضح</span></div></div></div> }
