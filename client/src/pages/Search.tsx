import { FormEvent, useState } from "react";
import { ArrowLeft, Search as SearchIcon, Sparkles } from "lucide-react";
import { useLocation } from "wouter";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export default function Search() { const [, setLocation] = useLocation(); const [username, setUsername] = useState(""); function submit(e: FormEvent) { e.preventDefault(); const clean = username.replace(/^@+/, "").trim(); if (clean) setLocation(`/profile/${clean}`); } return <div className="mx-auto max-w-5xl px-5 py-16 lg:px-8"><div className="mx-auto max-w-3xl text-center"><div className="mx-auto mb-5 grid h-14 w-14 place-items-center rounded-2xl bg-[#e9eee7] text-[#55785a]"><SearchIcon /></div><div className="inline-flex items-center gap-2 text-sm font-bold text-[#a9795b]"><Sparkles size={15} /> تحليل مرقاب</div><h1 className="mt-4 text-4xl font-black text-[#293029]">حلّل أي حساب عام</h1><p className="mt-4 text-lg leading-8 text-[#6c736a]">اكتب اسم المستخدم كما يظهر في Instagram، وسيعرض لك مرقاب ملخص الحساب وتاريخه.</p><form onSubmit={submit} className="mt-9 flex flex-col gap-3 rounded-2xl border border-[#d8dad3] bg-white p-2 shadow-[0_18px_60px_rgba(41,48,39,.08)] sm:flex-row"><Input autoFocus value={username} onChange={e => setUsername(e.target.value)} placeholder="@username" className="h-14 border-0 bg-[#f7f8f5] text-lg shadow-none sm:flex-1" /><Button type="submit" className="h-14 rounded-xl bg-[#2d3a30] px-8 text-base font-bold text-white hover:bg-[#405545]">بدء التحليل <ArrowLeft className="mr-2" size={18} /></Button></form><div className="mt-6 flex flex-wrap justify-center gap-5 text-sm text-[#7b8378]"><span>ملخص واضح</span><span>سجل مؤرخ</span><span>مصدر ووقت الفحص</span></div></div></div> }
